name: vibecloud
region: nyc
static_sites:
  - name: prototype
    git:
      repo_clone_url: https://github.com/Cyano47/unified-project-prototype.git
      branch: main
    build_command: npm ci && npm run build
    output_dir: dist
    environment_slug: node-js
    catchall_document: index.html
